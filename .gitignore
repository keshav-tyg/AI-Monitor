node_modules/
dist/
.DS_Store
tmp/
.worktrees/
desktop/.gradle/
desktop/**/build/
# Release artifacts produced by the runbook. Uploaded to CWS and GitHub
# Releases directly; never checked into git.
/local-focus-coach-extension-*.zip
/local-focus-coach-macos-*.zip
# Release runbook. Kept locally (recommended location:
# ~/.local/share/local-focus-coach/RELEASE.md) so the "how we publish this app"
# steps stay off the public repo.
/RELEASE.md
